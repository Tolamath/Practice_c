/* Print out the numeric value of the newline character
 * 
 * Call header file
 * define main function
 * initialize character
 * set character as '\n'
 * use the printf function
 * use the %d format specifier in the printf function
 * return 0; 
 */
 #include <stdio.h>
 int main(){
    char c1;
    c1 = '\n';
    printf("The numeric value of the newline character is: %d\n", c1);

    return 0;
 }