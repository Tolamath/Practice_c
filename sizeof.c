#include <stdio.h>
/**
 * main - to find the size of char, int and float(and maybe double)
 *
 *
 * Return:0 Always(Success)
 */
int main(void)
{
	printf("The size of char on this computer is : %lu\n", sizeof(char));

	return (0);
}
